{
  "name": "tagims-tagim-shell",
  "private": true,
  "scripts": {
    "check": "wrangler deploy --dry-run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "workerd"
    ]
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "wrangler": "^4.0.0"
  }
}
