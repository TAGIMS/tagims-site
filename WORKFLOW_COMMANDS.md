# Deprecated Workflow Command File

The canonical owner workflow protocol for this repository is now:

`WORKFLOW_COMMAND_PROMPT.md`

Before handling owner workflow commands, read and follow `WORKFLOW_COMMAND_PROMPT.md`.

Do not maintain separate command definitions in this file. This compatibility pointer exists only so older references do not silently diverge from the canonical workflow.
